program
int X , Y , XY ;

begin
   read X;
   Y = 0;

   while ![( X > 0 ) && (X < 100)] loop
      read XY;
      if [(XY >= 0) || (XY <= 0)] then
        write XY;
        else
        write XY;
      end;
      Y = Y+XY;
      X = X-1;
    end;

   write X,Y;

end
